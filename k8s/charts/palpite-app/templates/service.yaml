apiVersion: v1
kind: Service
metadata:
  name: {{ include "palpite-app.fullname" . }}
  labels:
    {{- include "palpite-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "palpite-app.selectorLabels" . | nindent 4 }}
